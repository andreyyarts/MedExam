<ContentControl x:Class="MedExam.Patient.Reports.Views.ReportField"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:views="clr-namespace:MedExam.Patient.Reports.Views">
    <ContentControl.Template>
        <ControlTemplate TargetType="ContentControl">
            <DockPanel Margin="0 2">
                <views:ReportFieldTitle Text="{TemplateBinding Tag}" />
                <views:BorderBottomBlackLine>
                    <TextBlock Text="{TemplateBinding Content}" FontStyle="Italic" FontSize="{TemplateBinding FontSize}"
                               HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" TextWrapping="Wrap" />
                </views:BorderBottomBlackLine>
            </DockPanel>
        </ControlTemplate>
    </ContentControl.Template>
</ContentControl>
